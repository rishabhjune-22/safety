<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- First group without title -->
    <group android:checkableBehavior="single">
        <item android:title="Settings">
            <menu>
                <item
                    android:id="@+id/nav_profile"
                    android:title="Profile Setting"
                    android:icon="@drawable/ic_profilesetting"
                     />

                <item
                    android:id="@+id/nav_setting"
                    android:title="App Setting"
                    android:icon="@drawable/appsetting" />
            </menu>
        </item>
    </group>

    <!-- Second group without title -->
    <group android:checkableBehavior="single">
        <item android:title="Enquiry">
            <menu>
                <item
                    android:id="@+id/About_us"
                    android:title="About Us"
                    android:icon="@drawable/ic_aboutus" />

                <item
                    android:id="@+id/contact_us"
                    android:title="Contact Us"
                    android:icon="@drawable/contactus" />
            </menu>
        </item>
    </group>

    <!-- Logout option outside of any group -->
    <item
        android:id="@+id/nav_logout"
        android:title="Logout"
        android:icon="@drawable/ic_logout" />

</menu>
